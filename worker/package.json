cat > package.json << 'EOF'
{
  "name": "ajker-news-worker",
  "version": "1.0.0",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "web-push": "^3.6.7"
  },
  "devDependencies": {
    "wrangler": "^4.127.1"
  }
}
EOF
